# 💳 تدفق شحن المحفظة - Wallet Topup Flow

مكون متكامل وقابل لإعادة الاستخدام لشحن المحفظة في تطبيق Absher Pay.

## 📁 هيكل الملفات

```
wallet_topup/
├── WalletTopupFlow.js          # المكون الرئيسي الذي يدير التدفق
├── PaymentMethodSheet.js       # Bottom Sheet لاختيار طريقة الدفع
├── AddCardScreen.js            # شاشة إضافة بطاقة جديدة
├── TopupAmountScreen.js        # شاشة إدخال المبلغ
├── AmountKeypad.js            # لوحة الأرقام المخصصة
├── TopupSuccessScreen.js      # شاشة النجاح
├── index.js                   # ملف التصدير
└── README.md                  # هذا الملف
```

## 🎯 الاستخدام

### الاستخدام الأساسي

```jsx
import React, { useState } from 'react';
import { WalletTopupFlow } from '../../common/components/wallet_topup';

const YourComponent = () => {
  const [showTopup, setShowTopup] = useState(false);

  return (
    <>
      <Button onPress={() => setShowTopup(true)}>شحن المحفظة</Button>

      <WalletTopupFlow
        visible={showTopup}
        onClose={() => setShowTopup(false)}
        hasSavedCard={false}
      />
    </>
  );
};
```

### Props

| Prop | النوع | الافتراضي | الوصف |
|------|------|---------|-------|
| `visible` | `boolean` | - | إظهار أو إخفاء التدفق |
| `onClose` | `function` | - | دالة تُستدعى عند إغلاق التدفق |
| `hasSavedCard` | `boolean` | `false` | هل لدى المستخدم بطاقة محفوظة؟ |

## 🔄 مراحل التدفق

### 1️⃣ اختيار طريقة الدفع (PaymentMethodSheet)
- يظهر كـ Bottom Sheet
- خيارات الدفع:
  - **أبل باي**: للدفع الفوري باستخدام Face ID/Touch ID
  - **بطاقة فيزا**: للدفع ببطاقة ائتمانية

### 2️⃣ إضافة بطاقة (AddCardScreen) - اختياري
- يظهر فقط إذا اختار المستخدم "بطاقة فيزا" وليس لديه بطاقة محفوظة
- حقول الإدخال:
  - رقم البطاقة (16 رقم)
  - اسم حامل البطاقة
  - تاريخ الانتهاء (MM/YY)
  - CVV (3 أرقام)
- معاينة البطاقة في الوقت الفعلي
- التحقق من صحة البيانات

### 3️⃣ إدخال المبلغ (TopupAmountScreen)
- عرض طريقة الدفع المختارة
- لوحة أرقام مخصصة
- عرض المبلغ بخط كبير
- زر تأكيد المبلغ

### 4️⃣ النجاح (TopupSuccessScreen)
- رسالة نجاح مع أيقونة متحركة
- عرض المبلغ المضاف
- تفاصيل العملية (رقم العملية، التاريخ، الوقت)
- ميزات الأمان
- زر العودة للرئيسية

## 🎨 الألوان المستخدمة

التدفق يستخدم الألوان المعتمدة من Absher Pay:

- **اللون الأساسي (Business)**: `#0055aa`
- **أبل باي**: `#1a1a1a`
- **النجاح**: `#10b981`
- **الخلفية**: `#f8f8f8`

## 🔧 المكونات الفردية

يمكنك استخدام المكونات الفردية إذا أردت:

```jsx
import {
  PaymentMethodSheet,
  AddCardScreen,
  TopupAmountScreen,
  TopupSuccessScreen,
  AmountKeypad
} from '../../common/components/wallet_topup';
```

## 📱 مثال كامل

```jsx
import React, { useState } from 'react';
import { View, TouchableOpacity, Text } from 'react-native';
import { WalletTopupFlow } from '../../common/components/wallet_topup';

const WalletScreen = () => {
  const [showTopupFlow, setShowTopupFlow] = useState(false);
  const [hasSavedCard, setHasSavedCard] = useState(false);

  return (
    <View className="flex-1">
      <TouchableOpacity
        onPress={() => setShowTopupFlow(true)}
        className="bg-[#0055aa] rounded-2xl p-4"
      >
        <Text className="text-white text-center font-bold">
          شحن المحفظة
        </Text>
      </TouchableOpacity>

      <WalletTopupFlow
        visible={showTopupFlow}
        onClose={() => setShowTopupFlow(false)}
        hasSavedCard={hasSavedCard}
      />
    </View>
  );
};

export default WalletScreen;
```

## ✨ الميزات

✅ تدفق سلس ومنظم
✅ تصميم متوافق مع Material Design
✅ دعم كامل للغة العربية (RTL)
✅ رسوم متحركة سلسة
✅ التحقق من صحة البيانات
✅ معاينة فورية للبطاقة
✅ لوحة أرقام مخصصة
✅ شاشة نجاح تفاعلية
✅ ألوان معتمدة من النظام
✅ مكون قابل لإعادة الاستخدام

## 🔒 الأمان

- جميع معلومات البطاقة تُشفّر بالكامل
- استخدام بروتوكولات أمان بنكية
- عدم تخزين CVV
- تشفير SSL/TLS للاتصالات

## 📝 ملاحظات التطوير

- يستخدم React Hooks (useState, useEffect)
- متوافق مع NativeWind v2
- يتطلب @expo/vector-icons
- يعتمد على مكون SvgIcons

## 🚀 التطوير المستقبلي

- [ ] إضافة دعم Google Pay
- [ ] حفظ البطاقات المتعددة
- [ ] إضافة تاريخ العمليات
- [ ] إضافة خيار الشحن التلقائي
- [ ] دعم المحافظ الرقمية الأخرى

---

**تم التطوير بواسطة**: Absher Pay Team
**التاريخ**: 2025
**الإصدار**: 1.0.0
