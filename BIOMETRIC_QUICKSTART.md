# 🚀 دليل البدء السريع - نظام البصمة

## خطوات سريعة للتشغيل والاختبار

---

## 1️⃣ التأكد من التثبيت

المكتبات مثبتة بالفعل! ✅

```bash
✅ expo-local-authentication@~17.0.8
✅ expo-secure-store@~15.2.0
```

---

## 2️⃣ تشغيل التطبيق

### على الهاتف المحمول (موصى به للبصمة الحقيقية):

```bash
# ابدأ التطبيق
npx expo start

# ثم امسح QR code من تطبيق Expo Go
```

### على المحاكي:

```bash
# iOS (يدعم Face ID/Touch ID)
npx expo start --ios

# Android (يدعم Fingerprint)
npx expo start --android
```

---

## 3️⃣ إعداد البصمة على المحاكي

### iOS Simulator:
1. افتح Settings
2. Face ID & Passcode > Enroll Face
3. في قائمة Simulator: **Features > Face ID > Enrolled**

### Android Emulator:
1. افتح Settings
2. Security > Fingerprint
3. أضف بصمة وهمية
4. استخدم زر البصمة في لوحة جانب المحاكي

---

## 4️⃣ اختبار النظام خطوة بخطوة

### السيناريو 1: مستخدم جديد

1. **افتح التطبيق**
   - اختر "محفظة أبشر أفراد" أو "محفظة أبشر أعمال"

2. **تسجيل الدخول**
   - أدخل رقم الهوية: `1130019514`
   - أدخل كلمة المرور: `Test@123`
   - اضغط "تسجيل الدخول"

3. **سيظهر حوار التفعيل** 🎉
   ```
   تفعيل بصمة الإصبع
   هل تريد تفعيل البصمة لتسجيل الدخول السريع؟
   ```

4. **اختر أحد الخيارات:**
   - "تفعيل البصمة" → ستُطلب منك البصمة
   - "ربما لاحقاً" → تستمر للتطبيق بدون تفعيل

### السيناريو 2: مستخدم فعّل البصمة

1. **أغلق التطبيق وافتحه مرة أخرى**

2. **ستجد زر جديد:**
   ```
   📱 تسجيل الدخول ببصمة الإصبع
      آمن وسريع عبر الضغط

   [زر البصمة الكبير] 👆

   الدخول ببصمة الإصبع
   ```

3. **اضغط الزر**
   - ستظهر واجهة البصمة
   - تحقق بالبصمة
   - ✅ دخول مباشر للتطبيق!

### السيناريو 3: التحكم من الإعدادات

1. **افتح صفحة الإعدادات**

2. **ابحث عن قسم "الإشعارات"**
   ```
   التحقق ببصمة الإصبع [Toggle]
   ```

3. **جرب التبديل:**
   - **إيقاف التشغيل:** سيسألك للتأكيد
   - **تشغيل:** سيطلب منك التحقق بالبصمة أولاً

---

## 5️⃣ حالات الاختبار

### ✅ يجب أن يعمل:
- [ ] عرض الحوار بعد تسجيل دخول ناجح (أول مرة)
- [ ] حفظ البيانات عند الموافقة على التفعيل
- [ ] إخفاء الحوار إذا رفض المستخدم
- [ ] عرض زر البصمة في شاشة تسجيل الدخول (بعد التفعيل)
- [ ] إخفاء زر البصمة إذا لم تكن مفعلة
- [ ] تسجيل دخول ناجح بالبصمة
- [ ] عرض Toggle في الإعدادات
- [ ] تفعيل البصمة من الإعدادات
- [ ] إلغاء البصمة من الإعدادات
- [ ] حذف البيانات عند تسجيل الخروج

### ❌ يجب أن لا يعمل (معالجة الأخطاء):
- [ ] عرض رسالة خطأ إذا الجهاز لا يدعم البصمة
- [ ] عرض رسالة إذا لا توجد بصمات مسجلة
- [ ] عرض رسالة إذا ألغى المستخدم التحقق
- [ ] عدم السماح بالتفعيل بدون التحقق بالبصمة

---

## 6️⃣ نقاط مهمة للاختبار

### على جهاز حقيقي:
✅ **الأفضل للاختبار الكامل**
- البصمة الحقيقية تعمل بشكل طبيعي
- Face ID يعمل على iPhone X وما بعدها
- Fingerprint يعمل على معظم أجهزة Android

### على المحاكي:
⚠️ **محدود ولكن مفيد للتطوير**
- iOS Simulator يحاكي Face ID/Touch ID
- Android Emulator يحاكي Fingerprint
- قد لا تكون التجربة مطابقة 100%

---

## 7️⃣ استكشاف المشاكل

### المشكلة: لا يظهر حوار التفعيل
**الحل:**
1. تأكد من أنك تسجل دخول لأول مرة
2. تحقق من أن الجهاز يدعم البصمة
3. راجع console logs

### المشكلة: زر البصمة لا يظهر
**الحل:**
1. تأكد من تفعيل البصمة
2. تحقق من: `await BiometricService.isBiometricEnabled()`
3. أعد تشغيل التطبيق

### المشكلة: "الجهاز لا يدعم البصمة"
**الحل على المحاكي:**
- **iOS:** Features > Face ID > Enrolled
- **Android:** Settings > Security > Add Fingerprint

**الحل على الجهاز:**
- تأكد من تسجيل بصمتك في إعدادات الجهاز

### المشكلة: التطبيق يتعطل عند الضغط على زر البصمة
**الحل:**
1. تحقق من console للأخطاء
2. تأكد من تثبيت المكتبات بشكل صحيح:
   ```bash
   npx expo install expo-local-authentication expo-secure-store
   ```
3. نظف الكاش:
   ```bash
   npx expo start --clear
   ```

---

## 8️⃣ كود سريع للاختبار اليدوي

افتح Metro Bundler Console واكتب:

```javascript
// فحص الدعم
import BiometricService from './src/common/services/BiometricService';

const test = async () => {
  const result = await BiometricService.checkBiometricSupport();
  console.log('الدعم:', result);
};

test();
```

---

## 9️⃣ بيانات اختبار

### حسابات التجربة:

**حساب أفراد:**
- رقم الهوية: `1130019514`
- كلمة المرور: `Test@123`

**حساب أعمال:**
- رقم الهوية: `1130019514`
- كلمة المرور: `Test@123`

---

## 🔟 الخطوات التالية

بعد التأكد من أن كل شيء يعمل:

1. ✅ جرب على جهاز حقيقي
2. ✅ اختبر جميع السيناريوهات
3. ✅ تأكد من رسائل الأخطاء واضحة
4. ✅ اختبر الأداء والسرعة
5. ✅ شارك التطبيق مع المستخدمين للتجربة

---

## 📞 الدعم

إذا واجهت مشكلة:
1. راجع console logs
2. تحقق من ملف `BIOMETRIC_GUIDE.md`
3. جرب الأمثلة في `BIOMETRIC_EXAMPLES.md`
4. تأكد من الصلاحيات في `app.json`

---

## 🎉 جاهز!

الآن أنت جاهز لاختبار نظام البصمة الكامل!

**ابدأ التطبيق وجرب:**

```bash
npx expo start
```

**استمتع بتجربة التطوير! 🚀**
